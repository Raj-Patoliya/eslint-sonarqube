{
  "name": "js",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint JS --ext .ts,.js --rulesdir JS/rules",
    "lint-fix": "eslint JS --fix --rulesdir JS/rules",
    "lint-report": "eslint JS --ext .ts,.js -f json -o eslint_report.json --rulesdir JS/rules"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.48.0"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "underscore": "^1.13.6"
  }
}